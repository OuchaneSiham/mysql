siham1=# select Distinct country_birth from person order by country_birth;

-- Distinct means ifd we have dup only retrive one of that tyep 
-- siham1=# select Distinct birth_date from person order by birth_date;

