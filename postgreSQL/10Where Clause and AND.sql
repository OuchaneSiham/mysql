select * from person WHERE country_birth = 'China' AND gender = 'Male';
-- so it should be both truie to be execiuted

select * from person WHERE country_birth = 'China' OR gender = 'Male';
-- whetever one of them should be true 
