USE sql_store ;
SELECT * FROM customers
WHERE  points < 0
-- // where we can give it a condition all the condition operators
WHERE  state = "name of state " .... lowerracse or uppercase 
-- does matter 
-- outside the city for example 
WHERE  state != 'va'  oe this notation <>

--  born after a date 
WHERE  birth_date > "the date here " 